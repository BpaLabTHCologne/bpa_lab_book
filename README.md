# The BPA Lab — Book

Online book documenting the architecture and operation of the **Business Process Automation Lab (BPA Lab)** at TH Köln (University of Applied Sciences).

Content is based on the [bpa_lab_docs](https://github.com/BpaLabTHCologne/bpa_lab_docs) repository and is published as a book in HTML, PDF, and ePub format using [Quarto](https://quarto.org/).

**Author:** Matthias Zapp, TH Köln

➡️ **Live version:** https://bpalabthcologne.github.io/bpa_lab_book/ *(after first deployment)*

---

## Prerequisites

- [Quarto](https://quarto.org/docs/get-started/) (version 1.4 or newer)
- Optional: TinyTeX for PDF output — install via `quarto install tinytex`
- Git

## Local preview

```bash
quarto preview
```

Quarto opens a browser tab with a live preview that re-renders on every file change.

## Full build

```bash
quarto render
```

The rendered output (HTML, PDF, ePub) lands in the `docs/` folder.

## Project structure

```
.
├── _quarto.yml                       # Central Quarto configuration
├── theme.scss                        # Light-mode style overrides
├── theme-dark.scss                   # Dark-mode style overrides
├── index.qmd                         # Overview / cover page
├── chapter01-introduction.qmd        # Introduction
├── chapter02-solution-overview.qmd   # Solution overview
├── chapter03-business-scenario.qmd   # Business scenario and sub-processes
├── chapter04-data.qmd                # Data and data sources
├── chapter05-architecture.qmd        # Software architecture (C4)
├── chapter06-decisions.qmd           # Architecture decisions (ADRs)
├── appendix-a-components.qmd         # Components and sister repositories
├── appendix-b-glossary.qmd           # Glossary
├── references.qmd                    # Bibliography page
├── references.bib                    # BibTeX bibliography
├── images/                           # Diagrams and graphics
├── .github/workflows/publish.yml     # Automated GitHub Pages deployment
└── docs/                             # Build output (generated by Quarto)
```

## Adding the images

The `.qmd` files reference images in `images/`. Please copy these from the [bpa_lab_docs](https://github.com/BpaLabTHCologne/bpa_lab_docs) repository. The required files are listed in `images/README.md`.

## Deployment via GitHub Pages

The included workflow at `.github/workflows/publish.yml` renders the book on every push to `main` and publishes the output to the `gh-pages` branch.

**One-time setup:**

1. Push this repository to GitHub.
2. Under **Settings → Pages**, set the source to *Deploy from a branch*, branch `gh-pages`, folder `/ (root)`.
3. The `gh-pages` branch is created automatically by the first action run.

## Licence

Content is published under **CC BY-SA 4.0**.

## Citation

> Zapp, Matthias (2026). *The BPA Lab — Architecture and Documentation of a Demonstration Factory for Business Process Automation and Analytics.* TH Köln (University of Applied Sciences). Online: <https://bpalabthcologne.github.io/bpa_lab_book/>.
